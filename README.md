# GraphQL Server Playground (graphql-server-playground)
Playground for running GraphQL requests.  Uses Apollo Server and TypeScript

# npm i

# npm run dev

# Open http://localhost:4000 and use sample query from examples.gql
